import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ 
  subsets: ['latin'],
  weight: ['300', '400', '500', '600', '700', '800', '900'],
  display: 'swap',
});

export const metadata: Metadata = {
  title: 'Marmodity Ventures LLP - Premium Fresh Fish Delivered Globally',
  description: 'Premium quality seafood from ocean to your table with sustainable practices. Fresh fish, salmon, prawns, and more delivered worldwide.',
  keywords: 'fresh fish, seafood export, salmon, prawns, sustainable fishing, global delivery',
  authors: [{ name: 'Marmodity Ventures LLP' }],
  viewport: 'width=device-width, initial-scale=1',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className="scroll-smooth">
      <head>
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossOrigin="anonymous" />
      </head>
      <body className={inter.className}>
        {children}
      </body>
    </html>
  );
}